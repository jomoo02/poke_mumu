import React from 'react';
import { IoIosClose } from '@react-icons/all-files/io/IoIosClose';

export default function CloseIcon({ size = '1rem', color = '#64748b' }) {
  return <IoIosClose size={size} color={color} />;
}
