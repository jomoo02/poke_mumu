'use client';

import { useEffect } from 'react';
import { setPokeCardIndex } from './actions/action';

export default function CookieSet({ basicInfo }) {
  useEffect(() => {
    // setPokeCardIndex(basicInfo);
  }, []);

  return null;
}
