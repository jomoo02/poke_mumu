const level = 'level';

const pre = 'pre';

const egg = 'egg';

const tutor = 'tutor';

const remidner = 'reminder';

const hm = 'hm';

const tm = 'tm';

const tr = 'tr';

const methods = {
  level,
  pre,
  egg,
  tutor,
  remidner,
  hm,
  tm,
  tr,
};

export {
  methods,
};
