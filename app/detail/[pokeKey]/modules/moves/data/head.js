const name = 'name';

const type = 'type';

const damageClass = 'damageClass';

const power = 'power';

const accuracy = 'accuracy';

const level = 'level';

const pre = 'pre';

const hm = 'hm';

const tm = 'tm';

const tr = 'tr';

const heads = {
  name,
  type,
  damageClass,
  power,
  accuracy,
  level,
  pre,
  hm,
  tm,
  tr,
};

export {
  heads,
};
